# ===========================================
# CONFIGURAZIONE AMBIENTE - PT SAAS FRONTEND
# ===========================================
# Copia questo file in .env.local e compila i valori

# URL API Backend
# In sviluppo: lasciare vuoto (usa il proxy Vite automatico)
# In produzione: impostare l'URL completo del backend (es. https://api.tuodominio.com/api)
VITE_API_URL=

# URL WebSocket (Socket.io)
# In sviluppo: lasciare vuoto (default: http://localhost:3000)
# In produzione: impostare l'URL del server WebSocket (es. https://api.tuodominio.com)
VITE_SOCKET_URL=
